html(lang="eng")
    head 
        title Products
    
    body 
    include ../partials/headers
    h1 All Products
    each item in product
        a(href="/products/"+item.id) Name: #{item.name} (ID: #{item.id}) 
        br
        br